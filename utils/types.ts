interface DashboardCardType {
  title: string;
  count: number;
}

interface NavlinksTypes {
  name: string;
  url: string;
}

interface ProjectCardType {
  title: string;
  description: string;
  url: string;
  image: string;
  github: string;
}
